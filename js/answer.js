

      var answers = [
        "it needs to STOP.",
        "we need to CHANGE for the BETTER."
      ];

        function fortune() {
          num = Math.round( (answers.length - 1) * Math.random());
          return answers[num];
        }

      var answers3 = [
        "it needs to STOP.",
        "we need to CHANGE for the BETTER."
      ];

        function fortune() {
          num = Math.round( (answers.length - 1) * Math.random());
          return3 answers[num];
        }

      var answers4 = [
        "Stop.",
        "Say it with me.",
      ];

        function fortune() {
          num = Math.round( (answers.length - 1) * Math.random());
          return answers4[num];
        }
